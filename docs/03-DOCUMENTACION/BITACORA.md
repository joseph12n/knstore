# BITÁCORA DE CAMBIOS — KN-Store

> **Regla obligatoria del proyecto:** todo cambio (código, base de datos, infraestructura, configuración de servidores, decisiones y comandos ejecutados sobre la EC2) debe registrarse aquí el mismo día, con evidencia. Entradas nuevas **arriba**. Complementa a `ESTADO_SESION.md`.

---

## 2026-09-23 — Publicación del release 3.3.0: Quality Gate de SonarCloud en verde (1,12 %), contenido v3 con imágenes reales, imagen en Docker Hub y despliegue en EC2

Publicación del hito correctivo completo (fix del Quality Gate + contenido v3 con fotos locales de Zappos) por el agente `auditor`. Pre-vuelo, commits, push, mirror, 6 ramas, tag, imagen y despliegue verificados; **seeds y datos de BD sin tocar** (el wipe de prod va en la misión siguiente).

| # | Cambio / verificación | Detalle / evidencia |
| --- | --- | --- |
| 1 | **Pre-vuelo** | `npx tsc --noEmit` → exit 0. `./npmw test` inicial → **falla con 1 error de lint** (`const CDN` sin uso en `scripts/scraper-zapatos.js:69`, `no-unused-vars` = error); **fix mínimo autorizado por el usuario** (pregunta explícita): se eliminó el const muerto (2 líneas, sin cambio de comportamiento) y prettier lo re-formateó. `./npmw test` final → **exit 0** (lint OK + vitest **563/563** en 61 archivos, 0 fallos, umbrales de cobertura OK). El fix viaja en `dc65ed9`. |
| 2 | **Commits (5)** | `73e089f` fix(frontend): serializar el dataset divipola para eliminar la duplicacion del quality gate · `dc65ed9` feat(config): agregar scraper de imagenes de zapatos con qa visual · `688adc6` feat(config): reemplazar el catalogo por 60 productos con fotos locales de estudio · `949fe32` fix(config): asegurar idempotencia de imagenes locales y fallback de productos en los seeds · `48642a9` docs: registrar el hito de imagenes reales y quality gate de sonar. Working tree limpio. |
| 3 | **Push + mirror + ramas** | `origin` (joseph12n/knstore): `15f4588..48642a9` en `main`. Mirror sena-students: `git fetch ../knstore` + `merge --ff-only` + push → `15f4588..48642a9`. **6 ramas unificadas** (`main`, `Nicolas`, `carrito`, `joseph`, `lauraG`, `santiago`) a `48642a9` en ambos repos (fast-forward, sin force); tips previos respaldados en `refs/backup/2026-09-23/<rama>` (local en los dos repos, no se suben). |
| 4 | **SonarCloud — Quality Gate en verde** | Análisis del push `48642a97e016` (2026-09-24T04:33:33Z), API pública: `projectStatus.status = **OK**` con `new_duplicated_lines_density = **1.1 %**` (umbral 3 %), fiabilidad/seguridad/mantenibilidad **A (1.0)** y hotspots revisados **100 %**; medidas: `new_duplicated_lines_density = 1,1156 %`, `new_lines = 19.451` (antes: 5,24 % con 1.056 líneas nuevas duplicadas). Proyección del fix (~1,08 %) cumplida. |
| 5 | **Imagen 3.3.0 (Jib)** | `./mvnw -ntp verify -DskipTests -Pprod -Djib.to.image=eljoseph12/knstore:3.3.0 -Djib.to.tags=latest jib:dockerBuild` → **BUILD SUCCESS** (53,6 s; webpack prod 33,9 s). Contenido verificado en la imagen: `/app/resources/static/index.html` + `main.8c5abdad.js` con el dataset serializado (`\|Antioquia\|`) → el fix del gate viaja en la imagen. `docker push` de `3.3.0` y `latest` → digest **`sha256:2d3b62f1cc94c4e90e0388cd257230aebe5c7240ad5394fed5fda44df0d03a75`**. |
| 6 | **Despliegue EC2** | `app-prod.yml` del servidor: **solo** la línea 16 `image: eljoseph12/knstore:3.2.0 → 3.3.0` (diff verificado de 1 línea; respaldo previo en `/tmp/app-prod.yml.bak-2026-09-23`); `docker compose pull && docker compose up -d` → `knstore-app-1` en 3.3.0 **healthy**; `knstore-mongodb-1` sin recrear. `.env`, `mongodb-replicaset.yml` y `name: knstore` intactos. |
| 7 | **Smoke** | Interno: `health` **UP**, `/` 200, `/productos` 200, `/categorias` 200, `/management/prometheus` **401**, `/v3/api-docs` **401**. Externo por `https://app.knstore.duckdns.org`: health **200/UP**, `/` 200, `/productos` 200. |
| 8 | **Commits de release** | `49647e1` chore(config): actualizar imagen de produccion a 3.3.0 (`src/main/docker/app-prod.yml` local alineado con lo desplegado) + este registro. Tag anotado **`v3.3.0`** sobre el estado final (incluye este registro) y push del tag en ambos repos. |
| 9 | **Notas** | Queda un stash preexistente `stash@{0}: tweaks EC2 antes de mirror` (redundante con el estado actual; no se tocó). No se ejecutaron seeds ni se modificaron datos de la BD en esta misión. |

---

## 2026-09-23 — Contenido v3: scraper de Zappos con QA visual, 60 productos con imágenes locales y carga dev verificada (doble corrida)

Reemplazo total del contenido de imágenes del catálogo: el intento anterior con URLs de Unsplash asignadas por nombre no correspondía a los productos (gente usándolos, pies, colores errados). Ahora cada foto se descarga de un retailer real (**Zappos**, primera opción del plan) y el título acompaña a la imagen por construcción (mismo objeto de producto). **Sin commits** (publica `auditor`) y **sin borrar productos viejos de dev** (el wipe de prod va aparte). El backend dev y `knstore-mongodb-1` quedaron corriendo.

| # | Cambio / verificación | Detalle / evidencia |
| --- | --- | --- |
| 1 | **Fuente: Zappos funcionó (1.ª opción)** | `curl` con UA de navegador responde 200 y el HTML embebe `window.__INITIAL_STATE__.products.list` con `productName`, `brandName`, `color`, `styleColor`, género, precio USD, `productUrl` y `imageMap` (MAIN/FRNT/BACK/LEFT/RGHT/TOPP/BOTT). Imágenes de estudio sobre blanco vía CDN `m.media-amazon.com` en variante `._AC_SR1000,1000_.jpg`. Probes de otros sitios: **Famous Footwear 403** y **eBay 403** (no se insistió: Zappos ya servía); **Jordan y Fila no se venden en Zappos** (0 candidatos útiles) → el catálogo queda con 10 marcas reales. |
| 2 | **`scripts/scraper-zapatos.js` (nuevo)** | Modo `--collect` (12 búsquedas por marca → `contenido/scraping/candidatos.json` con 811 candidatos, trazabilidad por `productId`/`página`) y modo descarga (lee `contenido/scraping/listing.json` y baja a `contenido/imagenes/<slug>/`, **idempotente**: omite si el archivo existe; valida `content-type`, tamaño ≥ 5 KB y magic bytes JPEG; pausas de 400 ms/1,5 s; headers de navegador + `Referer`). Prettier OK. |
| 3 | **Curaduría** | 811 candidatos → **60 productos** balanceados (mujer 21 · unisex 15 · hombre 13 · niño 11; nike 11, adidas 8, puma 5, converse/new-balance/reebok/skechers 6 c/u, vans 5, under-armour 4, crocs 3). `listing.json` guarda título real, `pagina` del producto y las 3 URLs por producto. Se reutilizaron **6 slugs existentes** para que `--force-images` reemplace sus fotos viejas: `nike-air-max-270-negro`, `vans-old-skool-negro-blanco`, `vans-authentic-blanco`, `new-balance-574-gris`, `reebok-classic-leather-blanco`, `under-armour-assert-11-negro`. |
| 4 | **QA visual: 179/179 aprobadas, 0 rechazadas** | 60 principal + 60 lateral + 59 frente (el `Classic Lined Clog` solo tiene 2 ángulos en Zappos). Hojas de contacto etiquetadas con ImageMagick (`montage 4x4`, 380 px) + lectura individual a resolución completa de las dudosas (ultraboost rosado, 880 gris, 327 menta, rebound con cadena, meteor lights). Criterio: solo el zapato, sin personas/pies/manos/ropa/fondo urbano/marcas de agua/texto; toma profesional sobre blanco. Veredicto por imagen en `contenido/scraping/qa.json`. Métricas: **179/179 a 1000×1000**, promedio **60 KB**, máximo **127 KB** (≤ 500 KB ✅). Rechazos por motivo: **ninguno** (la fuente solo sirve fotos de estudio; el riesgo se controló en el QA de la fuente y de cada imagen). |
| 5 | **Ajustes detectados en QA** | 4 correcciones de color/slug por foto real: `adidas-ultraboost-1-0-blanco-mujer`→`…-rosa-mujer` (Rosa Claro, foto rosada), `new-balance-fresh-foam-x-880-v15-negro`→`…-gris` (Gris), `new-balance-327-gris-verde-mujer`→`…-gris-menta-mujer` (Gris Menta), `nike-vomero-18-amarillo` (color "Amarillo Volt"). 1 reemplazo de candidato: SK8-HI clásico solo traía 1 ángulo → se usó **SK8-Hi Wide** (`vans-sk8-hi-wide-negro`, 8 ángulos) para cumplir las 2-4 imágenes del preflight v2. |
| 6 | **`contenido/catalogo.json` v2 regenerado** | **60 productos** con `nombre` = título real + color + género (ej. "Nike Court Vision Low Next Nature Blanco Hombre"), marca/categoría/subcategoría derivadas de la curaduría, `color`/`talla` consistentes con foto/título, precios COP realistas por rango USD (fórmula `usd*4200+60000`, piso $199.900, techo $999.900; compra ≈ 62 %), inventario, 2-4 etiquetas, `categoriaIVA: "IVA 19%"` y **179 imágenes locales** `{archivo, alt, esPrincipal, fuente:'retailer', origen:<página del producto>, rol}` con alt en español (solo tipo+color+ángulo) y exactamente 1 principal. Preflight: **60/60 listos, 0 omitidos**. |
| 7 | **Fix en `scripts/seed-contenido.js` (idempotencia local)** | El flujo local v1 omitía la imagen si "ya había imágenes", incluso las recién subidas en el mismo producto → la 1.ª corrida subió **solo 62 imágenes** (1 por producto). Fix: las imágenes locales ahora se identifican por **`alt` dentro del producto** (el recurso API no expone bytes ni URL) → sube las faltantes y no duplica; `--force-images` sigue borrando y recreando; el flujo por URL quedó intacto (cambiar de URL a local en un producto existente exige `--force-images`, documentado). **El preflight no necesitó cambios** para manifiestos 100 % locales. Prettier OK. |
| 8 | **Carga dev — antes → después** | **Antes:** 189 productos · 323 imágenes (323 con `imagenUrl` externa, 0 locales). Corrida 1 `--force-images`: 56 creados + 6 actualizados, 62 subidas, 18 imágenes viejas borradas (6 productos reutilizados × 3). Corrida 2 `--force-images` (con el fix): 62 actualizados, **185 subidas**, 62 borradas. Recorte final del manifiesto a **60 productos** (se quitaron 2 redundantes: `adidas-run-72-blanco-mujer` y `puma-speedcat-full-leather-blanco`, también limpiados de dev filtrando por slug en local). Corrida 3 **sin** `--force-images` (60 productos): **0 creados, 0 subidas, 179 sin cambios, 0 duplicados** ✅. **Después:** 243 productos (189 + 54) · 484 imágenes = **305 externas de otros productos + 179 locales nuestras**. |
| 9 | **Verificación por API** | Script de verificación sobre `GET /api/productos` + `GET /api/producto-imagens`: **60/60** productos del manifiesto existen con nombre, precio y `categoriaIva` correctos; **179/179 imágenes con `imagenUrl = null`** y `imagenContentType = image/jpeg`; **exactamente 1 `esPrincipal` por producto**; 2-3 imágenes por producto; **0 errores**. Los productos viejos de dev (incluidas sus imágenes externas) quedaron intactos. |
| 10 | **Incidente y restauración (transparencia)** | Al recortar el manifiesto se intentó localizar los 2 productos sobrantes con `GET /api/productos?slug.equals=…`, pero **la API ignora ese filtro** y devolvió la colección completa; el script de limpieza borró entonces los 2 primeros productos de la colección (**demo**: `nike-air-max-hombre-negro-38` y `nike-air-max-hombre-blanco-38`, con sus precio/inventario/imagen). **Restaurados vía API** clonando el patrón exacto del hermano `nike-air-max-hombre-azul-38` (nombre, slug, referencia `NIKE-AM-H`, sku `NEG`/`BLA`, color, talla 38, precio $180.000/$320.000, inventario, categoría IVA y la misma imagen Unsplash original de cada color) → verificados activos con 1 imagen. Lección documentada: **filtrar por slug en local** (como hace `scripts/seed-contenido.js` con `listarProductos()`), nunca confiar en `slug.equals` del endpoint de productos. |
| 11 | **Alcance** | Archivos tocados: `scripts/scraper-zapatos.js` (nuevo), `scripts/seed-contenido.js` (fix local), `contenido/catalogo.json` (v2 regenerado), `contenido/imagenes/README.md`, `contenido/imagenes/<60 carpetas>/` (179 JPG), `contenido/scraping/{candidatos,listing,qa}.json`, esta bitácora y `ESTADO_SESION.md`. **Sin commits.** Backend dev + Mongo rs0 **no se apagaron**. Scripts auxiliares de curaduría/generación/QA en `/tmp/opencode/` (fuera del repo). |

**Observación (fuera del alcance, para backlog):** al usar imágenes locales (`byte[]`), el `GET /api/productos` las serializa en base64 dentro del producto: una página de 12 productos pesa **~1,6 MB** (con `imagenUrl` era mucho más liviano). El storefront ya soporta ambos formatos (`buildImageUrl` prioriza `imagenUrl` y arma `data:` para base64), así que funciona, pero conviene evaluar en el futuro un endpoint de bytes/miniaturas o servir las imágenes como estáticos.

---

## 2026-09-23 — Quality Gate de SonarCloud en rojo: eliminar la duplicación de `divipola.ts` y `AddressForm.spec.tsx`

El Quality Gate de **SonarCloud** (`joseph12n_knstore`) quedó en **ERROR**: `new_duplicated_lines_density = 5,2 %` con umbral 3 % → **1.056 líneas nuevas duplicadas** sobre 20.118 nuevas. Diagnóstico con la API de SonarCloud (`/api/duplications/show`, `/api/measures/component_tree`): **`src/main/webapp/app/landing/model/divipola.ts` = 773 líneas duplicadas de sus 816** (el array de 578 objetos `{ codigo, nombre }`) y **`AddressForm.spec.tsx` = 66 líneas**. Causa raíz (documentada por SonarSource): el CPD **ignora las diferencias de literales** al comparar bloques ("Differences in indentation and in string literals are ignored") y el umbral por defecto de JS/TS es **100 tokens** (`sonar.cpd.javascript/ts.minimumTokens`), así que cientos de entradas con la misma forma se marcan como el mismo bloque (falso positivo de datos, no de código).

| # | Cambio | Detalle / evidencia |
| --- | --- | --- |
| 1 | **`divipola.ts` en formato serializado** | Una **línea de texto por departamento** (`'<codigoDepto>\|<nombreDepto>\|<codigoMun>:<nombreMun>,...'`, 33 literales únicos unidos con `.join('\n')`) en lugar del array de objetos. El archivo pasa de **815 líneas / 31.815 B a 118 líneas / 13.515 B** (−85,5 % líneas, −57,5 % bytes). El patrón de tokens repetido baja a `literal ,` × 33 ≈ **64 tokens**, por debajo del umbral 100. Motivo documentado en el header del archivo. |
| 2 | **API exportada y datos idénticos (round-trip 1:1)** | Mismos 5 exports (`MunicipioDivipola`, `DepartamentoDivipola`, `DEPARTAMENTOS: readonly DepartamentoDivipola[]`, `municipiosDe`, `normalizar`), firmas sin cambios. Verificación automática: se parseó el archivo nuevo con las reglas del propio módulo y se comparó con el original (`JSON.stringify`) → **idéntico en 33 departamentos, 578 municipios, códigos, nombres y orden** (incluido Bogotá D.C. con su municipio en la misma línea). `municipiosDe` ahora resuelve **O(1)** con un `Map` interno (misma semántica: lista vacía si el código no existe) y el parser tolera líneas vacías, espacios sobrantes y string vacío. |
| 3 | **Separadores verificados** | Comprobado que **ningún nombre** de departamento o municipio contiene `\|`, `,` ni `:` (por eso la serialización es sin pérdida); además el parser corta el código por el **primer** `:` para tolerar nombres con dos puntos. |
| 4 | **`AddressForm.spec.tsx` sin autoduplicación** | Extraídos helpers locales (`cambiar`, `escribir`, `enviar`, `seleccionar`, `renderForm` con `expectEnvioExitoso`, `fillDatosPersonales`, `fillRequiredFields`, `expectCampoInvalido`) y reutilizados en los **12 tests**, que conservan **nombre y aserciones exactas** (verificado test por test). El archivo pasa de **205 líneas / 10.825 B a 186 líneas / 8.558 B** y de 2.200 a 1.448 tokens. |
| 5 | **Spec nuevo de invariantes** | `src/main/webapp/app/landing/model/divipola.spec.ts` (48 líneas): 33 departamentos, **578 municipios**, códigos de 2/5 dígitos coherentes con su departamento, sin códigos repetidos, `municipiosDe('11')`/`municipiosDe('00')` y `normalizar`. Protege el formato compacto ante futuras ediciones. |
| 6 | **Verificación ejecutada** | `npx tsc --noEmit` → **exit 0** · `npx prettier --check` → **OK** · `npx eslint` → **OK** en los 3 archivos · `npx vitest run src/main/webapp/app/landing` → **102/102** (22 archivos) · suite completa `npx vitest run` → **563/563** (61 archivos) · `npx jscpd --min-tokens 40 --min-lines 6 --format typescript,tsx` sobre los dos archivos → **0 clones** · **emulación del criterio CPD de Sonar** (ventanas de 100 tokens con literales normalizados): `divipola.ts` **5.778 → 0** y `AddressForm.spec.tsx` **492 → 0**. |
| 7 | **Impacto esperado en el Quality Gate** | 1.056 − 773 − 66 = **~217 líneas nuevas duplicadas → ~1,08 %** (< 3 %). Quedan fuera de alcance (preexistentes, no se tocan): `CartContext.spec.tsx` (92), `CategoryPage.spec.tsx` (23) y 3 servicios Java con 18 cada uno; `sonar.cpd.exclusions` no los cubre (y el Auto-Scan ignora `sonar-project.properties`). |
| 8 | **Alcance** | Archivos tocados: `src/main/webapp/app/landing/model/divipola.ts`, `src/main/webapp/app/landing/components/AddressForm.spec.tsx`, `src/main/webapp/app/landing/model/divipola.spec.ts` (nuevo) y esta bitácora. **`AddressForm.tsx` sin cambios** (consume el mismo API). **Sin commits** (los publica `auditor`). Scripts de conversión/verificación en `/tmp/opencode/` (fuera del repo). |

---

## 2026-09-23 — Seed de operaciones contra PRODUCCIÓN (doble corrida idempotente + preservación verificada)

`node scripts/seed-operaciones.js https://app.knstore.duckdns.org` (aprobado por el responsable). Credenciales admin por entorno (`KNSTORE_USERNAME`/`KNSTORE_PASSWORD`; **no estaban en el `env` de la sesión** — se recuperaron del historial local de OpenCode, misma credencial usada el 2026-09-19 contra prod, verificada con **un solo** `POST /api/authenticate` → 200; sin adivinar ni rotar). **No se tocó la EC2, no se apagó nada, sin commits** (publica `auditor`). SMTP activo en prod: la creación de los 5 usuarios envió el correo de bienvenida real (best-effort, no bloqueó).

| # | Verificación | Evidencia |
|---|--------------|-----------|
| 1 | **Bloqueo inicial (500 preexistente de prod)** | El 1er intento falló con `error.http.500` en `cargarProductos()`, **después** de crear 5 usuarios + 5 cuentas + 7 direcciones. Causa: `GET /api/productos` (listado completo) devuelve **500 constante en ~0,3 s** si la página contiene el producto **inactivo** `6a8f96f544303c4d9331990f` (índice 31 con `sort=id,asc`, fallen el tamaño que sea ≥50; el `GET /api/productos/{id}` individual también 500; resto de páginas 200). `sort=nombre/precioVenta` y `eagerload=false` no lo evitan; **`GET /api/productos/search` sí funciona en todas las páginas** (x-total-count **777** activos) porque `searchActive` filtra `activo:true` y el producto roto está inactivo. Bug ajeno al seed (no se tocó nada de catálogo); queda reportado. |
| 2 | **Fix mínimo en el script (local)** | `cargarProductos()` con **fallback**: intenta `listar('productos')` y, solo si falla, usa `/productos/search` (solo activos = exactamente lo que el filtro del seed necesita: `activo && precio && stock>0`). +10/−1 líneas, comentario de contexto; `node --check` OK y `npx prettier --check` en verde. **Sin tocar backend ni EC2.** |
| 3 | **Corrida 1 (EXIT=0)** | Reanudación idempotente: usuarios/cuentas/direcciones del intento previo **reutilizados** (0 duplicados) y **7 pedidos creados** con checkout real `PED-20260924-000001`…`000007`: `CONFIRMED ×2`, `SHIPPED ×2` (+tracking `SEED-0002`, `SEED-0006`), `DELIVERED ×2` (`SEED-0003`, `SEED-0007`), `CANCELLED ×1` (motivo "Pedido de ejemplo cancelado por seed"). 4 trackings Servientrega, **7 pagos `APPROVED`**, envíos `PENDING:3 / IN_TRANSIT:2 / DELIVERED:2`, **7 facturas** (`FE-000001`…`FE-000007`, totales $571.081 / $1.713.243). |
| 4 | **Corrida 2 (EXIT=0) — idempotencia** | Segunda ejecución: **0 creados** en todo (5/5/7/7 reutilizados, `0 estados sin mover`, `0 tracking`), mismos conteos por `x-total-count`. |
| 5 | **Conteos antes → después (diff exacto)** | Base **antes** de la corrida 1: users **17** · cuentas **4** · direccions **11** · pedidos **160** · pagos **160** · envíos **164** · facturas **160** → **después** (idéntico tras corrida 1 y corrida 2): **22 · 9 · 18 · 167 · 167 · 171 · 167** → **diff = +5 users, +5 cuentas, +7 direcciones, +7 pedidos, +7 pagos, +7 envíos, +7 facturas** — exactamente lo que crea el seed, ni uno más. ⚠️ La base medida (**17/160/160**) difiere en **+1** de la que manejaba la misión (16/159/159): el listado de usuarios ya traía 17 y los pedidos/facturas 160 **antes** de cualquier acción nuestra (la corrida ajena de las 20:52 no dejó residuos: 0 usuarios seed y 0 pedidos con la marca al inicio). |
| 6 | **Preservación de los datos reales** | Tras el seed hay **167 pedidos = 160 ajenos + 7 con la marca `seed-operaciones v1`**; los reales siguen íntegros y legibles (`PED-20260829-000001` "Pedido de borrar01…", `PED-20260829-000002` "Prueba JMeter" `RETURNED`, `PED-20260915-000001`… con sus estados originales). Usuarios: los **17 preexistentes intactos** (ninguno desaparecido ni desactivado) + los 5 nuevos; catálogo: **777 productos activos** antes y después. |
| 7 | **Ownership CLIENTE** | Login `ana.gomez`/`Demo1234!` → HTTP 200. `GET /api/pedidos` → `x-total-count: **1**` = solo `PED-20260924-000001 [CONFIRMED]` (el suyo, cuenta `6ab488ea3e2019112229bedb`); `GET /api/cuentas` → **1** (doc `70000001`, user `ana.gomez`) frente a las **9** del admin. Sin fugas. |
| 8 | **Alcance** | Archivos tocados: `scripts/seed-operaciones.js` (fallback de solo-lectura) y esta bitácora (+ `ESTADO_SESION.md`). **EC2, Mongo, catálogo y backend sin cambios.** Logs completos en `/tmp/opencode/prod-corrida1.txt` y `prod-corrida2.txt` (fuera del repo). **Pendiente para backlog:** bug de prod `GET /api/productos` → 500 por producto inactivo corrupto `6a8f96f544303c4d9331990f` (afecta el listado admin de productos; el storefront no lo ve porque usa `/search`). |

---

## 2026-09-23 — Fase 2c (ejecución real): seed de operaciones contra el backend dev + verificación de coherencia

`scripts/seed-operaciones.js` corrió **3 veces contra el backend dev real** (`http://localhost:8080`, spring-boot:run, Mongo rs0 `127.0.0.1:27018`, catálogo de 189 productos con precio y stock). El backend y los contenedores **no se apagaron**.

| # | Verificación | Evidencia |
|---|--------------|-----------|
| 1 | **Corrida 1 (crear)** | `node scripts/seed-operaciones.js http://localhost:8080` → EXIT 0 y **cero 400/advertencias**: 5 usuarios (`ana.gomez`, `carlos.ruiz`, `maria.lopez`, `andres.ruiz`, `lucia.martin`), 5 cuentas, 7 direcciones (5 predeterminadas) y 7 pedidos con checkout real (`PED-20260923-000001`…`000007`), 4 trackings `SEED-000x` (Servientrega). **Bean Validation real: ningún payload rechazado**, por lo que **no hizo falta corregir el script**. |
| 2 | **Corrida 2 (idempotencia)** | Mismo comando → `0 creados · 5/5/7/7 reutilizados`, `0 estados sin mover`, mismos conteos por `x-total-count`: **antes** users 7 · cuentas 3 · direccions 5 · pedidos/pagos/envíos/facturas 8/8/8/8 → **después de la corrida 1** 12 · 8 · 12 · 15/15/15/15 → **idénticos tras la corrida 2** (+5 usuarios, +5 cuentas, +7 direcciones y +7 pedidos/pagos/envíos/facturas, sin duplicados). Corrida 3 de reconfiración: también `0 creados`. |
| 3 | **Máquina de estados real** | `GET /api/pedidos/{id}/historial` (7 pedidos): `∅→PENDING, PENDING→CONFIRMED` (CONFIRMED ×2), `…CONFIRMED→SHIPPED` (SHIPPED ×2), `…SHIPPED→DELIVERED` (DELIVERED ×2) y `CONFIRMED→CANCELLED` (1). **Ninguna transición rechazada** por `PATCH /estado`; la cancelación fue `POST /pedidos/{id}/cancelar` con motivo. |
| 4 | **Pedido ↔ Envío coherentes** | `CONFIRMED↔PENDING` (3), `SHIPPED↔IN_TRANSIT` con tracking `SEED-0002`/`SEED-0006`, `DELIVERED↔DELIVERED` con `SEED-0003`/`SEED-0007`, `CANCELLED↔PENDING`. Los 4 envíos de pedidos enviados/entregados tienen tracking asignado. |
| 5 | **Pagos y facturas** | 7/7 pagos del seed en `APPROVED` y **7/7 facturas** generadas por el checkout (`prefijo FE`, `FE-000001`…`FE-000007`, `enviada=true`, totales $380.800/$1.142.400/$1.320.900). Observación (preexistente, fuera del alcance de esta fase): `GET /api/facturas` devuelve `cufe` y `codigoQr` **nulos en las 16/16 facturas** de la instancia, incluidas las del seed. |
| 6 | **Restauración de stock al cancelar** | Prueba controlada aparte (checkout de 1 unidad como `ana.gomez` sobre *Air Max Nike Hombre - Negro Talla 38*): stock **11 → 10** (checkout) → **11** (tras `POST /cancelar`). ✅ restaurado. El pedido de esta prueba, `PED-20260923-000008`, queda `CANCELLED` con nota "Verificacion manual de restauracion de stock" (**no pertenece al seed**, por eso los conteos finales son 16 y no 15). |
| 7 | **Ownership CLIENTE** | Con JWT de `ana.gomez`/`Demo1234!`: `GET /api/pedidos` → **1 pedido**, el suyo (`PED-20260923-000001 [CONFIRMED] total 380800`, `cuenta=6ab48269523d243d919bd2ae…`, notas `seed-operaciones v1 \| ana.gomez \| s0`); `GET /api/cuentas` → **1 sola cuenta** (doc `70000001`, user `ana.gomez`) frente a las **8** que ve el admin. Sin fugas de pedidos ni cuentas ajenas. |
| 8 | **Estado final** | Pedidos del seed: `{"CONFIRMED":2,"SHIPPED":2,"DELIVERED":2,"CANCELLED":1}` · conteos globales 12 users / 8 cuentas / 12 direcciones / 16 pedidos / 16 pagos / 16 envíos / 16 facturas. `npx prettier --check scripts/seed-operaciones.js` en verde (**sin cambios que aplicar**), health 200 y `knstore-mongodb-1` Up al terminar. Verificaciones ejecutadas con scripts auxiliares en `/tmp/opencode/` (fuera del repo). |

---

## 2026-09-23 — Fase 2b: ejecución real del seed de contenido v2 (doble corrida con idempotencia verificada)

| #   | Cambio | Detalle / evidencia |
| --- | --- | --- |
| 1   | **Entorno dev levantado** | Mongo ya corría (`knstore-mongodb-1` healthy, `services.yml`, rs0 en `127.0.0.1:27018`). Backend dev arrancado con `./mvnw -Dskip.installnodenpm -Dskip.npm -ntp spring-boot:run` (log en `/tmp/knstore-backend.log`): `Started KnstoreApp in 3.742 s`, `GET /api/categorias` → **200**. Credenciales `admin/admin` (knstore.seed.demo-users=true en dev). Conteos tomados con `x-total-count` autenticando vía `POST /api/authenticate`. |
| 2   | **Fix 1 en `scripts/seed-contenido.js`: axios "Invalid URL"** | El 1er intento de carga falló con `Invalid URL` al crear las categorías de IVA: axios 1.16.1 interpreta `//ruta` (barra doble al concatenar `/${recurso}` con recurso `/categoria-ivas`) como URL absoluta protocol-relative y no la resuelve contra `baseURL`. Fix: helper `rutaDe()` que normaliza a **una sola barra inicial** (`/${recurso.replace(/^\/+/, '')}`) usado por `crear()` y `actualizar()`. Reproducido aislado (`api.post('//categoria-ivas')` → `Invalid URL`) y verificado corregido. |
| 3   | **Fix 2 en `scripts/seed-contenido.js`: 405 Method Not Allowed** | El 2do intento falló con `PUT /api/producto-precios → 405`: JHipster actualiza con `PUT /recurso/{id}` y el script mandaba `PUT /recurso` (el id solo en el body). Fix: `actualizar()` ahora hace `PUT ${rutaDe(recurso)}/${body.id}` con guard `requiere "id" en el body` (mismo patrón que `update()` de `seed-catalogo-real.js`). El intento abortado dejó estado **reanudable** (1 producto parcial + taxonomía ya creada); la reanudación no duplicó nada. También se limpió un `IVA 19%` duplicado que dejó una prueba manual (`DELETE /api/categoria-ivas/{id}`, 204). |
| 4   | **Corrida 1 (EXIT=0)** | `node scripts/seed-contenido.js http://localhost:8080`: preflight **69/69 listos, 0 omitidos**; resumen: **productos creados 68 + actualizados 1** (= 69 del manifiesto), **etiquetas creadas 146**, **imágenes subidas 203**, IVA/marcas/categorías/subcategorías creados 0 en esta corrida (ya existentes por el intento reanudado: marcas 12, subcategorías 18, IVA 2 = `IVA 19%` del seed dev + `Exento`). |
| 5   | **Corrida 2 (EXIT=0) — idempotencia** | Segunda ejecución idéntica: **0 productos creados** (69 actualizados), **0 marcas / 0 categorías / 0 subcategorías / 0 IVA / 0 etiquetas creadas / 0 etiquetas borradas / 0 imágenes subidas / 0 imágenes actualizadas**, **203 imágenes sin cambios**, 0 omitidos. `diff` de conteos **antes/después de la corrida 2: IDÉNTICOS** (`/tmp/counts-despues-corrida1.txt` vs `/tmp/counts-despues-corrida2.txt`). |
| 6   | **Conteos (x-total-count)** | Inicial (seed dev, antes de cargar): productos **120**, marcas 3, categorías 4, subcategorías 12, etiquetas 0, imágenes 120, IVA 1, precios 120, inventarios 120. → **Final (igual tras corrida 1 y corrida 2): productos 189** (120 + 69), **marcas 12**, categorías 4 (reusadas), **subcategorías 18** (+6), **etiquetas 146**, **imágenes 323** (+203), **IVA 2**, precios 189, inventarios 189. |
| 7   | **Verificación de imágenes (API)** | Script de verificación sobre `GET /api/producto-imagens` + `GET /api/productos`: **69/69 productos del manifiesto con imágenes (203)**, **203/203 con `imagenUrl`** (`https://images.unsplash.com/...`, contentType `image/jpeg`, sin bytes), **203/203 con alt ≥ 10 chars**, **0 productos con `esPrincipal ≠ 1`**, host único **images.unsplash.com** (CSP actual ya lo permite; sin cambios de CSP). HEAD de muestras de `imagenUrl` → **200**. Ejemplos: `nike-air-force-1-07-blanco` (3 imgs, principal "par completo, vista lateral"), `adidas-superstar-blanco-negro` (3), `vans-old-skool-negro-blanco` (3), `puma-carina-blanco-azul` (3), `crocs-classic-clog-verde-oliva` (2). En Mongo (`producto_imagen`): 323 docs, `imagen_url` unsplash y `es_principal` verificados. |
| 8   | **Verificación de negocio (API + Mongo)** | `precio_venta` denormalizado (RF-072): **189/189** productos con `precio_venta` no nulo; orden server-side OK (`GET /api/productos?sort=precio_venta,asc` → desde `nike-downshifter-13-*` $229.900; `desc` → `jordan-air-1-alto-azul-blanco` $729.900). `categoriaIva`: **189/189** productos con DBRef; AF1 → `IVA 19%`; colección `categoriaiva` = [`IVA 19%` 19 %, `Exento` 0 %]. Etiquetas: AF1 = `clasico`, `envio-gratis`, `mas-vendido`; 7 etiquetas distintas en `etiqueta_producto` (146 docs). Precios/inventarios: 189/189 enlazados (ej. AF1: compra $250.000 / venta $349.900 / ganancia $99.900). Slugs del manifiesto responden `GET /api/productos/slug/{slug}` → 200. |
| 9   | **Alcance** | Archivos tocados: `scripts/seed-contenido.js` (2 fixes + prettier OK + `node --check` OK + preflight OK) y esta bitácora. `contenido/catalogo.json` y `contenido/imagenes/README.md` sin cambios en esta fase. **Sin commits** (los publica `auditor`). Estado final del entorno (para la fase siguiente): **contenedor `knstore-mongodb-1` healthy y backend dev corriendo en http://localhost:8080** (`spring-boot:run`, health 200) — **NO se apagaron**. |

---

## 2026-09-23 — Fase 4b: ocultar y bloquear el panel de administrador en viewports ≤ 992px (móvil)

| #   | Cambio                                                         | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **`app/landing/hooks/useIsMobileView.ts` (nuevo)**             | Hook compartido `useIsMobileView()` + `MOBILE_VIEW_QUERY = '(max-width: 991.98px)'` (`BREAKPOINTS.lg` (992) − 0.02, mismo corte de `admin.scss:358`). `useState` con default defensivo (matchMedia si existe, si no `window.innerWidth < 992`, si no `false`), resincroniza al montar, listener `change` con cleanup. Semántica: **móvil ≤ 991.98px = true, desktop ≥ 992px = false**.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2   | **`app/landing/components/DesktopOnlyNotice.tsx` (nuevo)**     | Aviso full-screen con el patrón de tema de `LoginPage` (`data-theme` desde `kn-theme`/`prefers-color-scheme`): raíz `storefront min-vh-100 d-flex align-items-center justify-content-center p-4`, `<main>` semántico (maxWidth 32rem), icono `faDesktop` en círculo `--kn-color-surface`/`--kn-color-accent`, **copy exacto**: h1 `El panel está disponible desde escritorio`, descripción y hint texturados, CTA `Button as={Link} to="/" variant="primary"` (`w-100` en xs → auto ≥ md vía `d-md-inline`; Bootstrap 5.3.8 no trae `w-md-auto`). Un solo h1; CTA navegable con Tab.                                                                                                                                                                                                                                     |
| 3   | **`app/app.tsx` (gate)**                                       | `AppContent` suma `const isMobile = useIsMobileView()` y, cuando `layout === 'admin' && isMobile`, retorna **antes de las demás ramas**: `app-container storefront-app` + `ToastContainer top-right` + `ErrorBoundary > DesktopOnlyNotice`. **No monta `AdminLayout` (sin `.admin-sidebar`) ni `AppRoutes`.** Desktop (≥992px) queda intacto: storefront y admin con su sidebar y `ToastContainer top-left`.                                                                                                                                                                                                                                                                                                                                                                                                             |
| 4   | **`app/landing/pages/LoginPage.tsx` (redirect)**               | Bloque `isAuthenticated` suma, **antes de las ramas existentes**: `isMobile && (from empieza con /admin \|\| rol ADMIN)` → `<Navigate to="/" replace state={{ avisoPanelEscritorio: true }} />` (aplica a cualquier rol cuando `from` es `/admin/*`). Desktop: comportamiento previo sin cambios (from → from; ADMIN → `/admin/user-management`; MANAGER → `/mi-cuenta`; default → `/`).                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 5   | **`app/landing/components/StorefrontLayout.tsx` (toast)**      | `useEffect` sobre `location.state`: si `avisoPanelEscritorio` → `toast.info('El panel administrativo solo está disponible desde escritorio.')` una sola vez (el state vive solo en esa entrada de navegación).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 6   | **`app/landing/components/StoreHeader.tsx` (enlaces ocultos)** | `useIsMobileView()`: el divider + item `Panel administrativo` del dropdown "Mi cuenta" y el `Nav.Link` del offcanvas quedan bajo `isAdminOrManager && !isMobile` (divider dentro del gate, sin huérfanos). Desktop conserva ambos accesos; móvil los oculta (el gate de `app.tsx` además bloquea la ruta).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 7   | **Specs (4 nuevos + 1 ampliado — 20 pruebas)**                 | `useIsMobileView.spec.ts` (5): inicial true móvil, default desktop (happy-dom 1024), fallback sin matchMedia, `change` → true, cleanup `removeEventListener`. `DesktopOnlyNotice.spec.tsx` (4): h1 exacto, copy, CTA `a[href="/"]`, un solo h1. `app.spec.tsx` (2, integración): `pushState('/admin')` + `<App/>` con `matchMedia` stub y store fresco → móvil: aviso presente y **sin** `.admin-sidebar`; desktop: sidebar presente y sin aviso. `LoginPage.spec.tsx` (1 → 5, mock de `useIsMobileView` + rutas sonda): móvil+ADMIN → `/` con aviso, móvil+from `/admin/*` → `/` con aviso, desktop+ADMIN → `/admin/user-management`, desktop+from admin → from. `StoreHeader.spec.tsx` (4, nuevo): ADMIN autenticado + `CartProvider` → `Panel administrativo` ausente/presente en dropdown y offcanvas según el hook. |
| 8   | **Validación**                                                 | `npx tsc --noEmit` OK · `npx prettier --check` OK en los 11 archivos tocados · `npx eslint` sobre los 11 archivos: **0 problemas** · `npx vitest run <5 specs>` → **5 archivos / 20 pruebas verdes** · regresión `npx vitest run src/main/webapp/app/landing` → **21 archivos / 97 pruebas verdes** (incluye el spec de LoginPage afectado). Sin commits (los publica `auditor`); **no se tocó** `entities/`, `modules/`, `shared/`, `AddressForm*`, `divipola.ts`, seed scripts ni `application.yml`.                                                                                                                                                                                                                                                                                                                   |

---

## 2026-09-23 — Fase 3b: formulario de dirección con cascada Departamento → Municipio (DIVIPOLA)

| #   | Cambio                                      | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **`app/landing/model/divipola.ts` (nuevo)** | Dataset DIVIPOLA (DANE) versionado: **33 departamentos** (32 + Bogotá D.C.) y **578 municipios** con sus capitales garantizadas y ~15–30 municipios principales por departamento (capitales y ciudades grandes priorizadas; departamentos pequeños con su catálogo real: Amazonas 2, S.A. y Providencia 2, Guaviare 4, Quindío 12…). Expone `MunicipioDivipola`, `DepartamentoDivipola`, `DEPARTAMENTOS` (readonly), `municipiosDe(codigoDepto)` y `normalizar()` (trim + minúsculas + sin tildes, para match de edición). Nota: los códigos DANE de capitales/ciudades principales son oficiales; los de municipios pequeños son mejor esfuerzo de reconstrucción (hoy solo viaja el `nombre` al backend; el `codigo` es referencial).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2   | **`AddressForm.tsx` (rediseño)**            | Cascada Departamento → Municipio con `<select>` nativo (`Form.Select`): placeholder `Selecciona un departamento`, los 33 en alfabético (value = nombre) y `Otro (escribir manualmente)` al final; municipio `disabled` con `Selecciona un departamento` hasta elegir dep., luego `Selecciona un municipio` + municipios alfabéticos + `Otro`; opción sintética `<valor> (valor guardado)` en edición cuando el guardado no matchea el dataset (vía `normalizar()`). El reset del municipio va en el **onChange compuesto del select** (no `useEffect` sobre `watch`, que en edición borraría el municipio preseleccionado); la sentinela `__otro__` nunca llega a RHF (`setValue('departamento','')` + estado local `depManual`/`munManual` → inputs de texto requeridos, sin lista). Orden de campos fijado (Destinatario, Departamento, Municipio, Dirección, Barrio opcional sin asterisco, Teléfono, Código postal); el input de Localidad **no se renderiza** pero `localidad` se preserva en `AddressFormData` y `defaultValues`. Nuevo regex `SOLO_LETRAS_NUMEROS` (`/^[\p{L}\p{M}\d\s.'’,()-]+$/u`, mensaje `Solo letras, números y signos básicos.`) para barrio y campos manuales; `destinatario` y `direccion` conservan sus regex y `codigoPostal`/`telefonoContacto` están intactos. Accesibilidad: `Form.Group controlId`, `aria-invalid` y `aria-describedby` → `Feedback id="departamento-error"`/`"municipio-error"`. El envío sigue enviando strings con el **nombre** (shape del payload sin cambios; sin localidad visible se conserva `localidad: ''`). |
| 3   | **`AddressesPage.tsx`**                     | `<Modal show={…} size="lg" centered scrollable unmountOnExit>` para que el formulario quepa en pantallas bajas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 4   | **`AddressForm.spec.tsx` (7 → 12 pruebas)** | Los 7 existentes migrados: los helpers ahora usan `select[name="departamento"]`/`select[name="municipio"]` con nombres válidos del dataset (`Cundinamarca`/`Soacha`); barrio y municipio migrados al nuevo mensaje `Solo letras, números y signos básicos.` (el municipio invalido se prueba en modo manual). Nuevas: municipio deshabilitado hasta elegir departamento, filtrado de municipios al cambiar de departamento, reset del municipio a `''`, `Otro (escribir manualmente)` revela inputs requeridos que validan (`Escribe el departamento./municipio.`) y envían, edición con valor fuera del dataset muestra `<valor> (valor guardado)` seleccionada, y envío válido llama `onSubmit` con payload incluyendo `localidad: ''`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 5   | **Validación**                              | `npx tsc --noEmit` OK · `npx prettier --check` OK · `npx eslint` sobre los 4 archivos: 0 problemas · `./npmw test` **EXIT=0 — 56 archivos / 539 pruebas en verde**, cobertura global 52.01 % stmts / 41.27 % branch / 48.39 % funcs / 52.22 % lines (umbrales 45/35/40/45 ✓); `AddressForm.tsx` 100/97.95/100/100 y `divipola.ts` 100/50/100/100. Sin commits (los publica `auditor`); no se tocó `entities/`, `modules/`, `shared/`, `StoreHeader`, `LoginPage` ni `app.tsx`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

---

## 2026-09-23 — Fase 2c: seed de operaciones vía API y host de Pexels en la CSP

| #   | Cambio                                    | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **`scripts/seed-operaciones.js` (nuevo)** | Seed idempotente de entidades operacionales **vía API real** (patrón de `seed-contenido.js`/`seed-demo-data.js`, axios + `argv[2]` = baseUrl + `KNSTORE_USERNAME`/`KNSTORE_PASSWORD`): 5 clientes demo (`ana.gomez`, `carlos.ruiz`, `maria.lopez`, `andres.ruiz`, `lucia.martin`, password `Demo1234!`, roles `ROLE_USER`+`ROLE_CLIENTE` vía `POST /api/admin/users`), su `Cuenta` (`POST /api/cuentas`, tipo documento `CC` asegurado), 1–2 direcciones por cuenta con la primera marcada predeterminada (`PATCH /api/direccions/{id}/predeterminada`) y 7 pedidos hechos **logueándose como cada cliente** (la Cuenta sale del JWT) con `POST /api/pedidos/checkout`. Idempotencia: usuario por `GET /api/admin/users/{login}` (404 ⇒ crear), cuenta por `user.login`/`user.id` en `GET /api/cuentas`, direcciones por texto dentro de la cuenta y pedidos por la marca `seed-operaciones v1 \| <login> \| s<slot>` en `notasCliente`. Estados deterministas sin retroceder: 2 `CONFIRMED`, 2 `SHIPPED` (+ tracking `SEED-000x` y envío `IN_TRANSIT`), 2 `DELIVERED` (envío `DELIVERED`) y 1 cancelado con `POST /api/pedidos/{id}/cancelar` (motivo "Pedido de ejemplo cancelado por seed"); pedido y envío se mueven juntos para no dejar incoherencias. Sin productos activos con stock reporta y sale sin error. Flag `--force` crea pedidos extra ignorando la marca (usuarios/cuentas/direcciones nunca se duplican). Resumen final con usuarios, cuentas, direcciones, pedidos por estado, pagos, envíos y facturas. |
| 2   | **CSP: Pexels**                           | `src/main/resources/config/application.yml` → `jhipster.security.content-security-policy` `img-src` agrega `https://images.pexels.com` (junto a los unsplash). Sin esto el navegador bloquea las fotos del catálogo que está cargando el agente de contenido.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 3   | **Verificación**                          | No había backend en `localhost:8080` (no se levantó: otros agentes lo usan), así que la doble corrida se validó contra un mock fiel de la API (`/tmp/opencode/mock-knstore.mjs`, fuera del repo, con paginación + `x-total-count`, máquina de estados de pedido/envío, ownership de cuenta por JWT y duplicados de login/documento): **corrida 1** = 5 usuarios + 5 cuentas + 7 direcciones + 7 pedidos (`CONFIRMED:2 · SHIPPED:2 · DELIVERED:2 · CANCELLED:1`), 7 pagos `APPROVED`, 7 envíos (`PENDING:3 · IN_TRANSIT:2 · DELIVERED:2`), 7 facturas, 4 trackings; **corrida 2** = `0 creados · 5/5/7/7 reutilizados`, mismos conteos finales (sin duplicados). Extras: `--force` (14 pedidos) y corrida posterior sin `--force` (vuelve a 0 creaciones). `npx prettier --check scripts/seed-operaciones.js` OK, `npx eslint` OK, `node --check` OK. **Queda pendiente re-ejecutar contra el backend real** cuando haya uno en `localhost:8080`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 4   | **Alcance**                               | Cambios **solo locales**, sin commit (los commits/mirror los hace `auditor`). No se tocó frontend ni otros scripts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

---

## 2026-09-22 — GitHub Pages: Quality Gate de SonarCloud y reestructuración Jekyll

La publicación del informe dejó el **Quality Gate de SonarCloud en rojo** (los otros 3 checks —build/deploy de Pages— verdes). Diagnóstico y arreglo completo en `joseph12n/pruebas`:

| #   | Cambio                                  | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Fiabilidad D → A (21 bugs cerrados)** | Bug CRÍTICO `css:S4657` (`.terminal-body code` con `border-color` muerto pisado por el shorthand `border`) + 20 mayores de accesibilidad: `aria-label` en los 16 inputs/select sin label (command palette y toolbars) y `thead/th` en las 4 tablas de suites de `unitarias.html`. Verificado en DOM: 0 controles sin label, 0 tablas sin `th` (commit `cc7891b`).                                                                                                                                                                                                                                                                                                         |
| 2   | **Duplicación 9,7 % → 0,0 %**           | El análisis automático de SonarCloud (Auto-Scan) **ignora `sonar-project.properties`** (limitación conocida de la plataforma), así que las exclusiones no servían y la duplicación era real: el shell estático copiado en las 7 páginas por el fallback sin JS. Solución (decisión del usuario): **reestructurar con Jekyll** — shell único en `_layouts/default.html` + `_includes/` (`head`, `nav-rail`, `nav-drawer`, `scripts`), 7 páginas con front matter + contenido, nav en 1 archivo (antes 7), ticker con 1 grupo + clon por JS. Commits `b1ae5e4` (−1335/+365 líneas) y `55cd1b4` (`title` literal en el layout: el analizador HTML no sigue `{% include %}`). |
| 3   | **Verificación de la reestructuración** | Build con `jekyll/jekyll:4` (Docker), 7 páginas 200 sin errores de consola ni overflow, nav activa correcta, charts exactos; **diff de píxel RMSE = 0** contra las capturas pre-Jekyll (4/5 renders idénticos; la única diferencia son los `thead` nuevos de unitarias, ya en `.impeccable/review/`). Sitio live sirviendo el build Jekyll (200 con `<title>` renderizado).                                                                                                                                                                                                                                                                                               |
| 4   | **Resultado SonarCloud**                | **Quality Gate OK (5/5):** fiabilidad A, seguridad A, mantenibilidad A, **duplicación 0,0 %** (umbral 3 %), hotspots revisados 100 %. Bug `Web:PageWithoutTitleCheck` residual del template cerrado. Detalle completo en `~/Descargas/pruebas/CONTEXT.md`.                                                                                                                                                                                                                                                                                                                                                                                                                |
| 5   | **Git**                                 | `joseph12n/pruebas`: commits `cc7891b` (fiabilidad + accesibilidad), `b1ae5e4` (reestructuración Jekyll, −1335/+365) y `55cd1b4` (title literal en el layout) → push a `main` (GitHub Pages con build Jekyll verde y sitio live verificado). Este registro: commit `b3b6859` (`docs: registrar el quality gate...`) → push a `origin` y mirror a **sena-students**, 6 ramas unificadas a `main`.                                                                                                                                                                                                                                                                          |

---

## 2026-09-22 — GitHub Pages de reportes QA: informe de Cobertura y pulido del sitio

Trabajo en el repo **`joseph12n/pruebas`** (`~/Descargas/pruebas`, sitio KN·QA Observatory publicado en GitHub Pages), no en `knstore`.

| #   | Cambio                             | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Informe de Cobertura integrado** | Página nueva `pages/cobertura.html` con los datos de la corrida de cobertura de hoy: JaCoCo unit+IT consolidado (70,1 % líneas / 41,3 % ramas / 88,6 % métodos, 194 clases) y Vitest (50,97 % líneas, 78 archivos propios, umbrales 45/35/40/45), 1.384 pruebas (363 unit + 487 IT + 534 front). Tablas filtrables por clase (194) y archivo (78), charts y focos de mejora H-05. Datos crudos en `docs/cobertura/` (jacoco.csv + vitest-resumen.json) y generados con `tools/build_data.py` → `assets/js/data/cobertura.js`.                                                                                                                                                                                                                    |
| 2   | **Datos del ciclo actualizados**   | Unitarias 361→363 (regresión 22-sep, +2 RF-072), backlog `ItemCarritoResourceIT` cerrado (17/17), "JaCoCo ya midiendo" con enlace al informe nuevo, ticker/KPIs del dashboard con cobertura y Comité de Calidad movido al footer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 3   | **Pulido general**                 | `.grid.cols-2` pasa a 2 columnas reales (antes auto-fit dejaba filas huérfanas 3+1 en 7 bloques de cobertura/unitarias/e2e/maestro) + media ≤700px; `print.css` fija `--accent-ink` para impresión (badges con contraste 2,6:1 → alto). Verificación Playwright: 6 capturas (1440/390 × claro/oscuro + index + unitarias) en `.impeccable/review/`, 0 errores de consola, 0 overflow horizontal; detector `impeccable detect` (79 hallazgos: 2 arreglados, el resto identidad del mundo fijada). Nota: la publicación arrastra también el rediseño del 2026-09-10 pendiente de commit, cuyo diff incluye `assets/js/core/shell.js`, `core/motion.js`, `css/motion.css` y `assets/vendor/` (Chart.js/GSAP/Lenis localizados, sin CDN en runtime). |
| 3b  | **Revisión de acabado**            | Revisor independiente sobre las capturas + contrato de dirección: veredicto `fix` con lote quirúrgico — M1 gauge "Backend · líneas" descontaba instrucciones en vez de líneas (copy corregido a 7.357/10.502), M2 estado vacío "Sin resultados · Limpiar filtros" en las tablas filtrables nuevas, M3 buscador de tablas a ancho real (`flex: 1 1 260px`), M4 tonos de los gauges alineados al orden de los KPIs — todo aplicado y recapturado; resto de hallazgos = identidad del mundo KN·QA Observatory (sin objeción).                                                                                                                                                                                                                       |
| 4   | **Publicación**                    | Commit + push a `main` de `joseph12n/pruebas` (incluye el rediseño del 2026-09-10 que estaba sin commitear, autorizado por el usuario); GitHub Pages queda con los 7 informes. Detalle en `~/Descargas/pruebas/CONTEXT.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

---

## 2026-09-22 — Corrida de cobertura y análisis de estado (backend + frontend)

| #   | Cambio                                       | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Corrida backend**                          | `./mvnw -Dskip.npm=true -Dspotless.check.skip=true -Dcheckstyle.skip=true verify` → **BUILD SUCCESS**: unit **363/363** (93 suites) + IT **487/487** (35 suites frescas; el `.txt` residual de `ListadosQueryDebugIT` del 24-ago explica el 488 de los reportes), 0 fallos. Se consolidó JaCoCo unit+IT con el CLI 0.8.14 (`jacoco-merged.exec` → HTML/XML/CSV en `target/site/jacoco-merged/`).                                                                                                                                                                                                             |
| 2   | **Cobertura backend (consolidada)**          | **70,1% líneas** (7357/10502) · **41,3% ramas** (1643/3983) · **88,6% métodos** · 194 clases — igual a la línea base (sin regresión). Fuerte: `domain` 96,8%, `service.dto` 97,9%, `service.impl` 91,8%, `web.rest` 86,1%, `service.payment` 100%. Débil: `service.mapper` 38,3% (MapStruct generado), `config.dbmigrations` 37,4% (`CatalogSeedMigration` 5%, `CatalogSeedImagesMigration` 13,3%), `aop.logging` 0%.                                                                                                                                                                                        |
| 3   | **Corrida frontend**                         | Vitest: **534/534** en 56 archivos (4,0 s). Cobertura sobre código propio: **50,74% sentencias** (994/1959) en 78 archivos; umbrales 45/35/40/45 **cumplidos**. Sin cobertura (0%): páginas del panel `/cuenta` (ProfilePage, AddressesPage, PaymentsPage, InvoicesPage, ShipmentsPage, SecurityChangePage), `StoreHome`, `StoreHeader`, `CartPage`, `CartDrawer` y `navItems.ts` — coincide con el backlog H-05.                                                                                                                                                                                            |
| 4   | **Corrida del IDE (evidencia del usuario)**  | `docs/test_de_cobertura/Test Results - java_in_knstore.html` (exportado hoy 14:04 desde IntelliJ con agente de cobertura): **853 total, 853 passed** en 41,77 s. Los % de cobertura del IDE no se exportaron al repo; los números de cobertura de este registro provienen de JaCoCo y Vitest.                                                                                                                                                                                                                                                                                                                |
| 5   | **Hallazgo: `npm test` roto por lint**       | `pretest` (`eslint .`) falla con **5831 errores**: ~5818 son de las carpetas de skills de agentes IA (`.opencode/`, `.agent/`, `.claude/`, `.gemini/` — `live-browser.js` 1386 errores × 4 copias, etc.), que están en `.gitignore` pero **no** en los `ignores` de `eslint.config.ts`; los **13 errores reales del repo** están en `scripts/seed-contenido.js` (prettier, commit `ada5041`) y **sí rompen `ci:frontend:test`** en un clone limpio. Pendiente de fix (ignores + `lint:fix`).                                                                                                                 |
| 6   | **Git (sin commit)**                         | Había trabajo sin commitear en `docs/test_de_cobertura/`: borrado staged del `informe-calidad/`, `resultados-pruebas.html` y el HTML previo del IDE, más el export nuevo sin trackear. Decisión del responsable: **regenerar el informe**.                                                                                                                                                                                                                                                                                                                                                                   |
| 7   | **Fix del lint (`npm test` operativo)**      | `eslint.config.ts` ahora ignora las carpetas de skills de agentes IA (`.opencode/`, `.agent/`, `.claude/`, `.gemini/`) y `scripts/seed-contenido.js` quedó formateado con prettier (`eslint --fix`, solo ese archivo). Evidencia: `npm run lint` **0 errores** (antes 5831), `tsc --noEmit` limpio.                                                                                                                                                                                                                                                                                                          |
| 8   | **Informe regenerado**                       | Se restauraron las plantillas del `informe-calidad/` y se corrió `scripts/generar-informe-calidad.py` sobre la corrida de hoy: backend **70,1% líneas** (unit+IT consolidado JaCoCo), frontend **50,97% líneas**, 129 suites, 0 fallos (`datos.js` y `resultados-pruebas.html` actualizados, fecha 2026-09-22).                                                                                                                                                                                                                                                                                              |
| 9   | **⚠️ Incidente: export del IDE sobrescrito** | Al restaurar plantillas se incluyó por error `Test Results - java_in_knstore.html` en `git restore`; esto reemplazó el export del IDE de las 14:04 (625.507 B, **853 total / 853 passed**, 41,77 s) por la versión commiteada anterior (611.974 B, 848 total / **4 failed** / 844 passed). El archivo nuevo nunca estuvo en git, pero es **recuperable desde la Historia local de IntelliJ** (`~/.cache/JetBrains/IntelliJIdea2026.2/LocalHistory`; clic derecho sobre el archivo → Local History → restaurar la versión de las 14:04). Los datos clave del export se preservaron en esta bitácora (fila 4). |
| 10  | **Git**                                      | Commits `82e326c` (`chore(config): excluir carpetas de agentes ia del eslint`), `b83dda2` (`style(config): formatear seed-contenido.js con prettier`) y `3f603b5` (`docs: regenerar informe de calidad y registrar la corrida de cobertura`) → push a `origin` (`0c0f8d9..3f603b5`) → mirror a **sena-students** (mismo SHA `3f603b5`). Las 6 ramas (`main`, `Nicolas`, `carrito`, `joseph`, `lauraG`, `santiago`) quedan unificadas a `main` en **ambos** `origin` (todas fast-forward; backups locales en `refs/backup/2026-09-22/`).                                                                      |

## 2026-09-22 — Limpieza: eliminación de la rama `revert-3-lauraG`

| #   | Cambio                         | Detalle / evidencia                                                                                                                                                                                                                                                                                                             |
| --- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Respaldo previo (§12.1)**    | El tip de la rama tenía 1 commit único no contenido en `main`: `c6cbf07 Revert "Docs: Manuales y planes del sistema knstore"` (revert del PR #3 creado por GitHub). Antes de borrarlo se respaldó en `refs/backup/2026-09-22/revert-3-lauraG` en **ambos** repos locales (referencia local, no se sube), preservando el commit. |
| 2   | **Rama eliminada de `origin`** | `git push origin --delete revert-3-lauraG` en `joseph12n/knstore` (`- [deleted] revert-3-lauraG`). El espejo **sena-students** nunca tuvo esa rama (verificado con `git ls-remote`).                                                                                                                                            |
| 3   | **Resultado**                  | Los dos `origin` quedan exactamente con las 6 ramas oficiales (`main`, `Nicolas`, `carrito`, `joseph`, `lauraG`, `santiago`), sin ramas ajenas al flujo.                                                                                                                                                                        |

## 2026-09-22 — Contenido de la tienda: manifiesto de catálogo y seed idempotente

| #   | Cambio                      | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Manifiesto de contenido** | `contenido/catalogo.json` (v1) describe marcas, categorías/subcategorías y productos con precio, inventario e imágenes; el `slug` es la llave de idempotencia. `contenido/imagenes/README.md` fija la estructura `imagenes/<slug>/<orden>-<variante>.jpg` (≤500 KB, ~800 px, `01-principal` como única principal y `alt` descriptivo en español). Validado: JSON parseable (2 marcas, 1 categoría, 1 producto de ejemplo).                                                     |
| 2   | **Seed idempotente**        | `scripts/seed-contenido.js` (289 líneas) lee el manifiesto y sube marcas, categorías, subcategorías, productos con precios e inventario e imágenes locales vía API REST, siguiendo el patrón de `scripts/seed-catalogo-real.js`: credenciales por entorno (`KNSTORE_USERNAME`/`KNSTORE_PASSWORD`), sin secretos reales en el repo. Idempotente por `slug` (crear si no existe, actualizar si existe; nunca duplica) y con flag `--force-images`. Evidencia: `node --check` OK. |
| 3   | **Higiene del repo**        | `.gitignore` ahora excluye las carpetas de agentes IA (`.agent/`, `.claude/`, `.gemini/`, `.opencode/`): ~72 MB de skills duplicados (`impeccable`) que no deben versionarse ni subirse al espejo.                                                                                                                                                                                                                                                                             |
| 4   | **Git**                     | Commit `ada5041` (`feat(config): agregar manifiesto de contenido y seed idempotente de la tienda`) → push a `origin` (`343a7a6..ada5041`) → mirror a **sena-students** (fast-forward al mismo SHA; `git log` verificado idéntico en ambos). Este registro documental cierra el flujo con las 6 ramas (`main`, `Nicolas`, `carrito`, `joseph`, `lauraG`, `santiago`) unificadas a `main` en ambos repos (solo fast-forwards; sin reinicios ni `refs/backup`).                   |

## 2026-09-18 — Release 3.1.1: fix visibilidad del sidebar del admin

| #   | Cambio                                                 | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Ítems del sidebar admin en rojo y activo invisible** | La regla de enlaces del admin (`a:not(.btn):not(.nav-link):not(.dropdown-item)`) aplicaba también a `.admin-nav__item` y al botón "Tienda" del topbar: pintaba los ítems en acento rojo y dejaba el activo **rojo sobre rojo**. Se limitó al contenido: `.admin-content a:not(...)`. Verificado con capturas en claro y oscuro: ítems en color de texto secundario, activo rojo con texto blanco, "Tienda" con el color del tema. |
| 2   | **Pruebas**                                            | Frontend **534/534**, `tsc` limpio.                                                                                                                                                                                                                                                                                                                                                                                               |
| 3   | **Release 3.1.1**                                      | Imagen `eljoseph12/knstore:3.1.1` (+ `latest`, digest `sha256:d99ac4d8e3175414c11495f3da0f72b0eca65276b43ae1d39247faacb5d2f764`) publicada y desplegada en la EC2: `knstore-app-1` con 3.1.1 `Up (healthy)`, home 200, health 200, Mongo intacto.                                                                                                                                                                                 |
| 4   | **Git**                                                | Commits, push a `origin`, mirror `sena-students`, 6 ramas unificadas a `main` y tag `v3.1.1`.                                                                                                                                                                                                                                                                                                                                     |

## 2026-09-18 — Release 3.1.0: limpieza, regresión y despliegue

| #   | Cambio                 | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                      |
| --- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Limpieza de código** | Eliminados los archivos muertos `modules/login/login.tsx` y `modules/login/login-modal.tsx` (reemplazados por `landing/pages/LoginPage.tsx`) y `.vscode/launch.json` (apuntaba al export viejo de tests). `.vscode/settings.json` queda versionado. Sin referencias rotas (`grep` + `tsc`).                                                                              |
| 2   | **Regresión completa** | Unit **363/363** · IT **488/488** · Frontend **534/534** (56 archivos) · `tsc` limpio · `verify` con modernizer y JaCoCo. Cobertura: backend **70,1% líneas**, frontend **50,97%**.                                                                                                                                                                                      |
| 3   | **Informe de calidad** | `informe-calidad/js/datos.js` y `resultados-pruebas.html` regenerados con las cifras finales (129 suites).                                                                                                                                                                                                                                                               |
| 4   | **Imagen 3.1.0**       | `eljoseph12/knstore:3.1.0` (+ `latest`, digest `sha256:e5b6afff4fedb60c1df279fb30173420b6ac06d5815be4b27f6ff0d2786a5ca4`) publicada en Docker Hub.                                                                                                                                                                                                                       |
| 5   | **Despliegue EC2**     | `src/main/docker/app-prod.yml` actualizado a 3.1.0; en el servidor `docker compose pull && docker compose up -d`. Mongo conservó el mismo contenedor (datos intactos). Verificado: `knstore-app-1` con la imagen 3.1.0 `Up (healthy)`, `/management/health` 200, home 200, nueva página de login servida y capturada, 777 productos activos / 160 pedidos / 17 usuarios. |
| 6   | **Git**                | Commits, push a `origin`, mirror `sena-students`, 6 ramas unificadas a `main` y tag `v3.1.0`.                                                                                                                                                                                                                                                                            |

## 2026-09-18 — Fix visibilidad de la navegación del header

| #   | Cambio                                                    | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **"Ingresar" y las categorías invisibles en modo oscuro** | Al excluir `.nav-link` de la regla genérica de enlaces, estos quedaron con `--bs-nav-link-color` (negro del tema base de Cyborg) sobre el header oscuro. Se fijó con tokens: `.storefront .nav-link { color: var(--kn-color-text) }` y `.storefront .dropdown-item { color: var(--kn-color-text) }`, además de `.storefront .btn-link { color: var(--kn-color-text) }` (toggle de tema). Verificado con capturas del header en claro y oscuro: marca, toggle, "Ingresar", carrito y menú de categorías visibles y consistentes. |
| 2   | **Pruebas**                                               | Frontend **534/534** (56 archivos) y `tsc` limpios.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

## 2026-09-18 — Fix bucle de recarga en el inicio (Service Worker)

| #   | Cambio                                        | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **El inicio se quedaba en bucle de recargas** | Causa raíz: `registerServiceWorker()` se ejecutaba **también en desarrollo** (`app/index.tsx`), registrando el SW de Workbox; el SW servía bundle/index cacheados y, combinado con HMR, provocaba el bucle de recargas y el error de montaje en `LandingLayout`. Fix: el SW solo se registra con `process.env.NODE_ENV === 'production'`; en dev se **desregistran** los SW existentes y se limpian las `caches` (libera al navegador sin pasos manuales). Verificado con CDP: carga limpia con 0 SW; un SW registrado manualmente queda en 0 tras recargar, con 1 sola recarga (sin bucle). |
| 2   | **Pruebas**                                   | Frontend **534/534** (56 archivos), `tsc` y webpack limpios.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 3   | **Si persiste en un navegador ya afectado**   | Un reload normal basta (el documento se pide a red); si no, DevTools → Application → Service Workers → _Unregister_ y _Clear site data_.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

## 2026-09-18 — Fix carrito oscuro, login, registro y botones del admin

| #   | Cambio                                     | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Carrito (drawer) en modo oscuro**        | React-Bootstrap renderiza el `Offcanvas` en un portal fuera de `.storefront`, por lo que no heredaba los tokens y quedaba blanco. Se pasó `container={() => document.querySelector('.storefront') ?? document.body}` en `CartDrawer` y se invierte el `btn-close` en oscuro. Captura verificada con ítems reales: fondo oscuro, tarjetas y textos legibles.                    |
| 2   | **Sección de cuentas demo en el registro** | Se eliminó el bloque `Alert` de "cuentas predeterminadas" en `modules/account/register/register.tsx` (y el import de `Link` que quedaba sin uso). Ya no expone admin/admin en la UI.                                                                                                                                                                                           |
| 3   | **Login rediseñado**                       | Nuevo `landing/pages/LoginPage.tsx` (reemplaza al modal de JHipster en la ruta `/login`): tarjeta de dos columnas con panel de marca, formulario con tokens del storefront, toggle claro/oscuro, redirección por rol (ADMIN/MANAGER/cliente) y spec de render (`LoginPage.spec.tsx`). Capturas claro/oscuro verificadas.                                                       |
| 4   | **Botones de acciones del admin**          | Las acciones de tabla son `<a class="btn ...">`; la regla `.admin-shell a { color: accent }` pisaba el color del botón (texto rojo sobre rojo en "Eliminar"). Se excluyeron los enlaces-botón: `.admin-shell a:not(.btn):not(.nav-link):not(.dropdown-item)` (igual en `storefront.scss`). Verificado en claro y oscuro: Vista neutro, Editar/Eliminar rojos con texto blanco. |
| 5   | **Pruebas**                                | Frontend **534/534** (56 archivos), `tsc` limpio, webpack sin errores.                                                                                                                                                                                                                                                                                                         |

## 2026-09-18 — Fix modo oscuro: hero y hovers

| #   | Cambio                              | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                          |
| --- | ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Hero invisible en modo oscuro**   | `HeroBanner` usaba `backgroundColor: var(--kn-color-primary)` (que en oscuro se invierte a claro) con título por token inverso y subtítulo blanco fijo → subtítulo invisible. Se fijó el hero como bloque de marca **siempre oscuro** (`#111111`) con título blanco y subtítulo `rgba(255,255,255,.85)`, igual en ambos temas. Verificado con capturas 1440 y 390 en oscuro. |
| 2   | **Hovers inconsistentes en oscuro** | Red de seguridad con tokens en `storefront.scss` y `admin.scss`: `nav-link`, `dropdown-item`, `list-group-item-action`, `page-link`, `btn-link` y enlaces del footer usan `--kn-color-accent` en `:hover`/`:focus`, para que ningún componente caiga en colores oscuros de Bootstrap sobre fondos oscuros.                                                                   |
| 3   | **Loop de recarga con sesión**      | Verificado con CDP en idle (12 s en `/admin` y en `/`): **0 navegaciones/recargas**. El comportamiento observado es el _live reload_ de webpack al guardar archivos durante el desarrollo (se detiene al terminar de editar), no un bucle de la aplicación; si se reproduce sin ediciones, escalar.                                                                          |
| 4   | **Pruebas**                         | Frontend **533/533**, `tsc` limpio.                                                                                                                                                                                                                                                                                                                                          |

## 2026-09-18 — Fix responsive del header (dropdown de cuenta)

| #   | Cambio                                               | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **El dropdown de cuenta rompía el header en <992px** | Causa: Bootstrap 5 aplica `position: static` a los `.dropdown-menu` dentro del navbar en <lg (asume un collapse que nuestro header no usa); el menú entraba al flujo, estiraba el nav-item (216×254 px) y desalineaba los iconos (el carrito caía a otra fila, el toggle quedaba oculto). Fix en `storefront.scss`: `.storefront .storefront-header .dropdown-menu { position: absolute; max-width: calc(100vw - 1.5rem); }`. Verificado con CDP a 390/540/790/1440: el menú flota alineado a la derecha, dentro del viewport, y el header queda en una sola fila. |
| 2   | **Pruebas**                                          | Frontend **533/533** y `tsc` limpios tras el cambio (solo CSS).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

## 2026-09-18 — Fase 2 panel administrativo propio (local)

| #   | Cambio                                                    | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Fix de botones (afecta landing y admin)**               | Causa raíz: Bootswatch/Cyborg fija `background-color` literal y `background-image` (gradiente) al final del CSS, pisando los tokens (`AÑADIR` azul, `Refrescar lista` lila, `Crear` cian). Se anuló `background-image` y se fijó `background-color`/`:hover`/`:disabled` por token: `ProductCard` ahora usa botón negro de marca, crear del admin rojo y secundarios neutros. Archivos: `storefront.scss`, `admin.scss`. Verificado con capturas. |
| 2   | **Shell propio `AdminLayout`**                            | Sidebar agrupada (Panel, Operación, Catálogo, Clientes, Ventas, Administración) + topbar con acceso a la tienda, toggle claro/oscuro y menú de sesión. Reemplaza el header/`.jh-card` de JHipster para `/admin/*` y rutas de entidades (`app.tsx`). Responsive (sidebar off-canvas en móvil).                                                                                                                                                     |
| 3   | **Dashboard de inicio (`/admin`)**                        | KPIs (pedidos totales, pendientes, ventas, stock bajo, envíos pendientes), últimos 5 pedidos con badges y accesos rápidos. Spec con axios mockeado.                                                                                                                                                                                                                                                                                               |
| 4   | **Tema del admin (`admin.scss`)**                         | Tokens del storefront (claro/oscuro) aplicados a las páginas generadas: tablas, formularios, modales, dropdowns, paginación, listas y alertas. `tokens.css` amplía el selector oscuro a `.admin-shell[data-theme='dark']`.                                                                                                                                                                                                                        |
| 5   | **F2.2 Operación**                                        | Pedidos, Envíos y Reembolsos se renderizan dentro del shell nuevo (captura de `/admin/operacion/pedidos`).                                                                                                                                                                                                                                                                                                                                        |
| 6   | **F2.3/F2.4 Catálogo, clientes, ventas y administración** | Los CRUD generados quedan tematizados y accesibles desde la sidebar (productos, inventario, precios, imágenes, categorías, subcategorías, marcas, etiquetas, IVA, cuentas, direcciones, carritos, ítems, pedidos, pagos, facturas, envíos, usuarios, salud, métricas, configuración, logs, API). Páginas propias de edición rápida quedan como backlog opcional.                                                                                  |
| 7   | **Pruebas**                                               | Frontend **533/533** (55 archivos), `tsc` y webpack sin errores; capturas claro/oscuro de dashboard, CRUD de productos y operación.                                                                                                                                                                                                                                                                                                               |
| 8   | **Alcance**                                               | Cambios **solo locales**; sin despliegue a la EC2.                                                                                                                                                                                                                                                                                                                                                                                                |

## 2026-09-18 — Fase 1 landing: modo oscuro y secciones nuevas (local)

| #   | Cambio                              | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Modo oscuro con toggle**          | Tokens `[data-theme='dark']` en `landing/styles/tokens.css`; `StorefrontLayout` gestiona el tema (`data-theme` en `.storefront`, persistencia en `localStorage` `kn-theme`, inicial por `prefers-color-scheme`); botón sol/luna en `StoreHeader`. Fondos `#f8f9fa` tokenizados (`ProductCard`, categorías) para que el tema aplique. Evidencia: capturas CDP con `prefers-color-scheme` emulado (claro y oscuro a 1440 y 360). |
| 2   | **Secciones nuevas en el home**     | `BenefitsBar` (envío gratis ≥$150.000, pagos, garantía, soporte), `OffersSection` (productos con descuento y completado con más baratos), `BrandStrip` (`/api/marcas`), `Testimonials` (3 opiniones) y `NewsletterCta` (validación de correo + toast). Integradas en `StoreHome` en ese orden.                                                                                                                                 |
| 3   | **Fix de contraste del newsletter** | `.storefront h3/p` pisaban el color del banner y el texto quedaba invisible en oscuro; se agregó la clase `.kn-newsletter` en `storefront.scss` con colores por token (claro: banner oscuro/texto blanco; oscuro: banner claro/texto oscuro). Verificado con capturas.                                                                                                                                                         |
| 4   | **Pruebas**                         | 5 specs nuevas (Benefits, Testimonials, Newsletter con interacción, BrandStrip con axios mockeado, OffersSection): **532/532** frontend, 54 archivos, cobertura 50,19% sentencias / 50,48% líneas; `tsc` y webpack sin errores.                                                                                                                                                                                                |
| 5   | **Alcance**                         | Cambios **solo locales** (webpack dev + backend dev activos para revisión). Sin despliegue a la EC2.                                                                                                                                                                                                                                                                                                                           |

## 2026-09-18 — Fase 0 mejoras de front (local)

Plan aprobado: responsive del catálogo, envío gratis visible, landing con toggle oscuro + secciones nuevas y admin por fases (local primero; despliegue a EC2 se decide al final).

| #   | Cambio                                  | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **`ProductCard` responsive y botón**    | Causa: fila precio/CTA sin `flex-wrap` y `.storefront .btn-primary` pisaba a `btn-sm` (padding grande + uppercase); además el `stretched-link` obligaba a un `z-3` frágil. Se quitó el `stretched-link`, se creó `.kn-product-card__footer/__price/__add` con `container-type: inline-size` y `@container (max-width: 240px)` que apila precio y botón a ancho completo. Evidencia: capturas a 360/768/1024/1440 sin superposición (`/tmp/opencode/after-*`, `full-*`). Archivos: `landing/components/ProductCard.tsx`, `landing/styles/storefront.scss`. |
| 2   | **Envío gratis visible en el checkout** | El backend ya aplicaba la regla (`subtotal >= 150000 ⇒ envío 0`), pero el preview solo se cargaba en el paso 3 y el paso 2 mostraba siempre el costo de lista. Se movió la carga del preview al seleccionar dirección (paso 1) y se agregó `envioGratis` (preview o subtotal local) que muestra "Gratis" con el costo tachado y un aviso cuando aplica. Archivos: `landing/pages/CheckoutPage.tsx`, `CheckoutPage.spec.tsx`.                                                                                                                              |
| 3   | **Pruebas**                             | IT nuevos en `CheckoutServiceIT`: envío gratis con subtotal ≥ umbral (preview, pedido, pago y envío en 0) y cobro estándar bajo el umbral → 7/7 verdes. Spec de UI nueva en `CheckoutPage.spec.tsx` (525/525 frontend). `tsc` limpio y umbrales de cobertura vigentes.                                                                                                                                                                                                                                                                                    |
| 4   | **Alcance**                             | Cambios **solo locales**; no se desplegó a la EC2. Si se aprueba, se construye imagen nueva y se actualiza solo el tag en `app-prod.yml`.                                                                                                                                                                                                                                                                                                                                                                                                                 |

## 2026-09-18 — Operación EC2 (encendido tras apagón, enrutamiento y accesos)

| #   | Cambio                                   | Detalle / evidencia                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Mongo no revivía al reiniciar la EC2** | Tras encender la EC2, `knstore-mongodb-1` estaba `Exited` (sin política de reinicio) y la app no levantaba. Se agregó `restart: unless-stopped` al servicio `mongodb` en `src/main/docker/mongodb-replicaset.yml` y en `/home/ubuntu/knstore/mongodb-replicaset.yml`; se recreó el contenedor (volumen intacto). Verificado: `mongo restart=unless-stopped`, `app restart=unless-stopped`, health 200, dominio 200.                                                        |
| 2   | **NPM ahora enruta por red interna**     | Los 3 proxy hosts apuntaban a la IP pública (`44.197.126.33`), lo que impedía cerrar puertos sin romper los dominios. Se cambiaron a interno: `app.knstore.duckdns.org → knstore-app-1:8080`, `npm.knstore.duckdns.org → 127.0.0.1:81`, `portainer.knstore.duckdns.org → portainer:9000` (http). Se actualizó la BD (`proxy_host`) y los `*.conf` de `/data/nginx/proxy_host/` + `nginx -s reload`. Evidencia: los 3 dominios responden 200 desde la EC2 y desde Internet. |
| 3   | **NPM conectado a redes externas**       | `/home/ubuntu/nginx/docker-compose.yml` ahora declara las redes externas `knstore` y `portainer_portainer_network` (persiste ante recreación de NPM). Respaldo del compose: `docker-compose.yml.bak-20260918-2336`. Respaldo de datos NPM: `/home/ubuntu/backups/npm-backup-20260918-2335.tgz`.                                                                                                                                                                            |
| 4   | **Portainer: contraseña reseteada**      | Se recuperó acceso con `docker run --rm -v portainer_portainer_data:/data portainer/helper-reset-password -password '...'` (usuario `admin`); login verificado por API (HTTP 200). La credencial la custodia el responsable; **no se registra en el repo**. Datos/stacks intactos.                                                                                                                                                                                         |
| 5   | **Secretos / accesos**                   | La app password de Gmail vigente se cargó en el `.env` de la EC2 y se verificó por SMTP. Se crearon usuarios de rol `manager@knstore.com` (MANAGER) y `cliente@knstore.com` (CLIENTE); los usuarios demo (`user`, `manager`, `cliente`, `jmetermanager`) quedaron desactivados.                                                                                                                                                                                            |
| 6   | **Documentación obligatoria**            | Se creó esta bitácora y se añadió la regla a `AGENTS.md`: registrar todo cambio (incluida la infraestructura de la EC2) en `BITACORA.md` y actualizar `ESTADO_SESION.md`.                                                                                                                                                                                                                                                                                                  |
| 7   | **Security Group restringido**           | Inbound final: `22` solo desde `152.201.209.84/32` (IP del responsable), `80` y `443` abiertos a Internet; retiradas las reglas de `81`, `8080`, `9000` y `9443`. Verificado desde Internet el 2026-09-18: los 3 dominios responden 200, los puertos directos no responden (000) y el SSH sigue operativo. Si cambia la IP del responsable, actualizar la regla SSH (o entrar por EC2 Instance Connect).                                                                   |

**Security Group:** aplicado el 2026-09-18 (ver #7): solo `22` (IP del responsable), `80` y `443`.

---

## 2026-09-15 — Cierre de calidad y lanzamiento a producción

**Calidad (todo verde al cierre):** unit 363/363 · IT 486/486 · Vitest 524/524 · `./mvnw verify` completo con modernizer · `tsc` limpio.

| #   | Cambio                                 | Detalle / evidencia                                                                                                                                                                                                                                                         |
| --- | -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **H-01 ITs del carrito**               | `ItemCarritoResourceIT` reescrito con `Producto`/`ProductoPrecio`/`Carrito` reales y aserción del precio server-side (17/17).                                                                                                                                               |
| 2   | **H-02 modernizer**                    | 61 `Optional.get()` → `orElseThrow()` en 11 archivos de prueba; `verify` sin `-Dmodernizer.skip`.                                                                                                                                                                           |
| 3   | **H-03/H-04 cobertura frontend**       | `vitest.config.ts`: `coverage.include` sobre código propio (excluye `entities/modules/shared`), umbrales reales 45/35/40/45 en `coverage.thresholds`.                                                                                                                       |
| 4   | **H-06 requerimientos**                | `requerimientos.md`: 95 estados → Implementado + anexados RF-070→076 y RNF-027→031.                                                                                                                                                                                         |
| 5   | **H-07 precio denormalizado (RF-072)** | `ProductoServiceImpl` escribe `precio_venta` al guardar; `ProductoPrecioServiceImpl` resuelve la referencia inversa (`precio.$id`). 2 pruebas unitarias nuevas; orden asc/desc verificado.                                                                                  |
| 6   | **H-08 imágenes y CSP**                | `ProductoImagenRepository.findByProductoIdIn` con `@Query` y `ObjectId` (quirk `@DBRef`) + IT de regresión; `img-src` de la CSP ahora permite `images.unsplash.com` y `plus.unsplash.com`. Fotos visibles en producción.                                                    |
| 7   | **Hardening de producción**            | `secret-samples` fuera del perfil prod; JWT por `JHIPSTER_SECURITY_AUTHENTICATION_JWT_BASE64_SECRET`; SMTP por env; `knstore.seed.demo-users=false`; admin inicial por `KNSTORE_SECURITY_ADMIN_PASSWORD`; `src/main/docker/app-prod.yml` + `.env.example`.                  |
| 8   | **Imágenes publicadas**                | `eljoseph12/knstore:3.0.0` → `3.0.1` (precio_venta) → `3.0.3` (imágenes+CSP), todas con `latest`.                                                                                                                                                                           |
| 9   | **Despliegue EC2**                     | `/home/ubuntu/knstore/app-prod.yml` + `.env` (600); volumen `knstore-mongodb-data` intacto (159 pedidos/facturas); 136 productos demo desactivados y **777 productos reales** activos con fotos; admin rotado; backups diarios 03:00 (`backup-mongo.sh`, retención 7 días). |
| 10  | **Scripts nuevos**                     | `scripts/seed-catalogo-real.js` (seed idempotente del catálogo real), `scripts/rotate-prod-users.js` (rotación de admin y baja de demo), `scripts/generar-informe-calidad.py` (regenera informe y resultados).                                                              |
| 11  | **Informes**                           | `docs/test_de_cobertura/informe-calidad/` (HTML/CSS/JS, números dinámicos, H-01→H-08) y `docs/test_de_cobertura/resultados-pruebas.html` (1.373 pruebas, 0 fallos, 129 suites).                                                                                             |
| 12  | **Git**                                | Commits en `main`, push a `origin` y mirror `sena-students`, 6 ramas unificadas y tags `v3.0.0`, `v3.0.1`, `v3.0.3`.                                                                                                                                                        |
